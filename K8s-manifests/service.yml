apiVersion: v1
kind: Service
metadata:
  name: github-actions-service
spec:
  type: LoadBalancer
  selector:
    app: demo-app
  ports:
    - port: 80
      targetPort: 3000
      nodePort: 30007